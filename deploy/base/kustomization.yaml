apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: troshka

resources:
  - namespace.yaml
  - backend-config.yaml
  - backend-secret.yaml
  - backend-deployment.yaml
  - frontend-deployment.yaml
  - frontend-route.yaml
