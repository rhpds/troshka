{{- if and .Values.deploy .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "troshka.namespace" . }}
  labels:
    {{- include "troshka.labels" . | nindent 4 }}
{{- end }}
