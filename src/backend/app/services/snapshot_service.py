def capture_vm_disks(library_item_id: str, project_id: str, vm_node_id: str) -> None:
    """
    Background task to capture VM disk snapshots.

    Args:
        library_item_id: ID of the LibraryItem to update
        project_id: ID of the project containing the VM
        vm_node_id: ID of the VM node in the topology
    """
    pass
