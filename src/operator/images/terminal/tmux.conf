# Cluster terminal tmux defaults. This runs inside wetty (a browser terminal), so
# enable mouse support (wheel-scroll + pane select) and a generous scrollback —
# without these, scrolling back through `oc` / `watch` output in a browser is
# painful. Kept intentionally minimal; users can override via ~/.tmux.conf.
set -g mouse on
set -g history-limit 10000
