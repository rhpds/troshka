[pytest]
# Guardrail: fail any test that runs longer than 60s instead of hanging.
# The whole suite runs in ~4s, so this only trips on a genuine hang (e.g. a
# mock that lets code fall into the real _wait_for_datavolume 3600s poll loop).
timeout = 60
